[project]
name = "multi-agent-builder"
version = "0.1.0"
description = "Claude Code configuration library: subagent personas, skills, and commands for designing and prototyping multi-agent system architectures"
requires-python = ">=3.11"

[tool.pyright]
pythonVersion = "3.11"
